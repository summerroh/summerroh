$(document).ready(function() {
  
  var scrollLink = $('.nav-link,.nav-brand');
  
  // Smooth scrolling
  scrollLink.click(function(e) {
    e.preventDefault();
    $('body,html').animate({
      scrollTop: $(this.hash).offset().top - 70
    }, 1000 );
  });
})